using UnityEngine;

//public class changecolor : MonoBehaviour
//{
    //public GameObject movingObject; // Assign the bird object in the Unity Inspector
   // private SpriteRenderer spriteRenderer;

  //  private void //Start()
   // {
        // Get the SpriteRenderer component from this GameObject
    //    spriteRenderer = GetComponent<SpriteRenderer>();
   // }

    //void //Update()
   // {
        // Check if the right mouse button was clicked
       // if (Input.GetMouseButtonDown(1)) // 1 is for right click
     //   {
        //    // Convert mouse position to world position
         //   Vector2 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
         //   RaycastHit2D hit = Physics2D.Raycast(mousePos, Vector2.zero);

            // Check if the raycast hit this GameObject
          //  if (hit.collider != null && hit.transform.gameObject == movingObject)
         //   {
                // Change the color of the movingObject GameObject to red
            //    movingObject.GetComponent<SpriteRenderer>().color = Color.red;

                // Stop the movement of the Bird object
              //  Bird1 birdScript = movingObject.GetComponent<Bird1>(); // Access the Bird script on the moving object
              //  if (birdScript != null)
            //    {
             //       birdScript.StopMoving(); // Stop the bird's movement
            //    }
           // }
      //  }
   // }
//}
